// Basic Vehicle Type Boolean Controls
export type VehicleControls = {
  forward: boolean;
  backward: boolean;
  left: boolean;
  right: boolean;
  jump_brake: boolean;
  run: boolean;
  reset: boolean;
};
