// Basic Vehicle Type Boolean Controls
export type VehicleControls = {
  forward: boolean;
  backward: boolean;
  leftward: boolean;
  rightward: boolean;
  jump_brake: boolean;
  run: boolean;
  reset: boolean;
};
